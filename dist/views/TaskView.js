"use strict";
// class TaskView {
//     display(tasks: TaskModel[]) {
//         for (const task of tasks) {
//             console.log(`${task.id}: ${task.description} - ${task.done ? 'Done' : 'Pending'}`);
//         }
//     }
// }
